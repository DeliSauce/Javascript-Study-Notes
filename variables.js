// node (global variable)
global.myGlobal = "it's a small world, after all";

// browser (global variable)
window.myGlobal = "it's a small, small world";

/*
const:
- can't reassign
- mutable: so if this is an object, we can change the properties of it
- block scoped

let:
- block scoped

*/
